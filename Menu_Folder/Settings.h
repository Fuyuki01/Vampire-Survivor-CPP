#pragma once
#include "../Constants.h"

struct Settings
{
    int musicVolume = GameConstants::DEFAULT_MUSIC_VOLUME;
    bool musicStatus = GameConstants::DEFAULT_MUSIC_STATUS;
    bool fullscreen = GameConstants::DEFAULT_FULL_SCREEN_STATUS;
};